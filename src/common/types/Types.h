#pragma once

#include <vector>
#include <string>
#include <algorithm>
#include <any>
#include <map>

template <class T>
using Vector = std::vector<T>;

using String = std::string;
template <class T, class U>
using Map = std::map<T, U>;
